import React, { FunctionComponent } from "react";

import './styles.css'

const Empty: FunctionComponent = () => (
  <div className="empty-feed">
   <p>This feed seems to be empty.</p> 
  </div>
);

export default Empty;
